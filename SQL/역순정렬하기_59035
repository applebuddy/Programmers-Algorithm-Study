-- MARK: - 역순 정렬하기 59035

-- * SELECT를 통해 출력하고자 하는 열을 선택합니다. (NAME, DATETIME)
-- FROM을 통해 출력하려는 테이블ID를 선택합니다.
-- ORDER BY 를 통해 ANIMAL_ID 기준 역순(DESC)으로 출력합니다.
-- * 오름차순은 ASC, 그 반대는 DESC로 정렬이 가능합니다.

-- SQL 코드 ▼

SELECT NAME, DATETIME FROM ANIMAL_INS ORDER BY ANIMAL_ID DESC
